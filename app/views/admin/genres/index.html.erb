<div class="container m-5">

  <div class="row my-3">
    <div class="col-sm-10 px-sm-0 offset-sm-1">
      <h2 style="background-color:#F5F5F5;">ジャンル一覧</h2>
      </div>
      </div>

<%= flash[:notice] %>
<!--ジャンル作成-->
<%= render 'form', genre: @genre %>

  <!--ジャンル一覧-->
  <% @genres.each do |genre| %>
  <div class="card">
  <div class="card-body">
    <%= genre.genre_name %>
    <%= link_to "編集", edit_admin_genre_path(genre.id) %>
  </div>
</div>
<% end %>
</div>
 >